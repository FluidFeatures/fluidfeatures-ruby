common:
  base_uri: base_uri
  cache:
    enable: false
    dir: cache_dir
    limit: 2mb

development:
  app_id: development_app_id
  secret: development_secret

test:
  app_id: test_app_id
  secret: test_secret

production:
  app_id: production_app_id
  secret: production_secret
